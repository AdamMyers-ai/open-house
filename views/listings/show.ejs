<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= listing.owner.username %>'s listing</title>
  </head>
  <body>
    <%- include("../partials/_navbar.ejs") %>

    <h1>A listing in <%= listing.city %></h1>

    <h2>Details</h2>
    <p>House size: <%= listing.size %> sq ft</p>
    <p>Price: <%= listing.price %></p>
    <p>Street Address: <%= listing.streetAddress %></p>

    <h2>Owner</h2>
    <% if(listing.owner._id.equals(user._id)) { %>
    <p>You own this!</p>

    <form action="/listings/<%= listing._id %>/edit" method="GET">
      <button type="submit">Edit this property</button>
    </form>

    <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete this Property</button>
    </form>
    <% } else { %>
    <p>You don't own this, <%= listing.owner.username %> does!</p>
    <% } %>

    <h2>Favorites</h2>

    <p>Favorited by <%= listing.favoritedByUsers.length %> people.</p>

    <% if(userHasLikedItem) { %>
    <p>You have already liked this item.</p>

    <form
      action="/listings/<%= listing._id %>/favorited-by/<%= user._id %>?_method=DELETE"
      method="POST"
    >
      <button type="submit">Unfavorite this Listing</button>
    </form>
    <% } else { %>
    <form
      action="/listings/<%= listing._id %>/favorited-by/<%= user._id %>"
      method="POST"
    >
      <button type="submit">Favorite this Listing</button>
    </form>
    <% } %>
  </body>
</html>
